# @waggle/hive-mind-hooks-cursor

> **STUB.** Wave 2/3 implementation pending.

This package is a placeholder in the monorepo migration so that subtree-split + dependency-graph tooling sees the package boundary. Runtime functionality lands in a future sprint when the `cursor` client's hook surface is implemented.

See `packages/hive-mind-hooks-claude-code` for the Wave 1 reference shape.

License: Apache-2.0.
