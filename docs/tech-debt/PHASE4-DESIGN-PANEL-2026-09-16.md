<!-- Provenance: generated evidence, not a ratified plan. -->

> **Status.** Machine-generated Phase 4 design panel: six read-only scouts produced 30 proposals,
> each judged by three adversarial lenses (behavior preservation, Rule 8 coverage, design
> philosophy) for 90 votes; 24 of 30 were refuted. Recorded as evidence the way the Phase 3
> error-handling audit was.
>
> Only the TD-CHAT-23 governance half of this plan has been executed (commits `e42d4e27`,
> `ed4a6a8a`, `f3d21adf`). Everything else is a proposal awaiting founder ratification — the
> decision list is §6, and §7 lists facts the panel could not settle. One of those, F2, has since
> been verified by hand and contradicts the trust-half scout: see TECH-DEBT.md TD-CHAT-31.
>
> **Its §5.0 id table is no longer the allocator (2026-09-16).** Two ids were transcribed
> into `docs/TECH-DEBT.md` with different subjects than §5.0 reserved, and both are frozen
> by commit history, by `// QUIRK` markers in test source, and by `docs/TESTING.md` and the
> journey tracker. The ledger meanings win; the two panel subjects moved instead:
> panel `TD-CHAT-31` (dangling `chat.turn.start`) is now **TD-CHAT-43**, and panel
> `TD-CHAT-32` (`usesNamedWorkspace` cross-wiring) is now **TD-CHAT-44**. Every other
> panel id (`TD-CHAT-33`…`TD-CHAT-42`, `TD-TEST-8`) kept its number. Below, lines 117,
> 176, 287, 320 and 357 still spell the panel's `TD-CHAT-31`/`TD-CHAT-32` — read those as
> 43/44. §5.0's "Highest existing at HEAD" line is a snapshot of `ea82185d` and is stale:
> **`docs/TECH-DEBT.md` is the only safe source for the next free id**, which is
> `TD-CHAT-45` / `TD-TEST-10` / `TD-REL-6` / `TD-ENV-2` as of `93326e03`.
> Line numbers are at `ea82185d`.

# Phase 4 Execution Plan — `/remove-technical-debt`, software-design-philosophy lens
**Branch** `chore/tech-debt-phase4-chat-deep-modules` · **HEAD** `ea82185d` (= main `8b983077` + one docs-only commit) · `packages/server/src/local/routes/chat.ts` = 6024 lines
**Inputs** 6 scout reports, 30 claims, 90 refuter votes (3 lenses each). **No claim carried `verdict=INCOMPLETE`** — every claim got a full three-lens panel, so there is no unverified-panel section. There *is* one unverified **factual dispute** and three unverified **measurements** — §7.

---

## 0. How to read this

`REFUTED` in the panel almost never means "drop it". Of the 24 refuted claims, 15 were refuted **as specified** (direction sound, corrections mandatory) and 9 were refuted **decisively** (premise false or Rule-8-blocked). §2 carries the first group in commit order; §3 carries the second with the decisive reason so each becomes a recorded no-change or ledger row, never a silent drop.

Three structural facts govern everything below:

1. **Rule 8 binds harder than the scouts assumed.** Four of the 24 claims wrote a *false gap* into a proposed Safety Net Map edit (asserting code is unpinned when it is pinned). That is the inverse error of a Rule-8 breach and is equally damaging: it licenses a later agent to touch covered code believing there is no net. Every Map edit in §2 has been re-checked.
2. **The helper set's pins are not typechecked.** `packages/server/tsconfig.json` excludes `tests`, `@typescript-eslint/no-unused-vars` is off, and there is no `noUnusedLocals`. A missed field rename in a pin file **fails open**, not closed. Every structure commit that renames a bag field needs an explicit tests-inclusive typecheck gate.
3. **Five scouts independently claimed `TD-CHAT-30`.** IDs are allocated centrally in §5.0.

---

## 1. Phase 4 score

### 1.1 Per module

| Module | Lines (HEAD) | Score | Failed diagnostic rows | What it needs |
|---|---|---|---|---|
| `validateChatRequestFields` | 103-167 | **7.25** | 1 (0.75), 3 (0.50), 4 (0.50), 7 (0.75) | Row 4: rewrite the doc block — it claims validation happens "before any referenced resource is resolved" while 135-137 resolves `selectedSkill` against installed skills (409). Row 3: the 8-field all-`unknown` input bag (74-83) is re-packed by the caller at 2582-2594, so adding a validated field touches three places. Row 7: the `assertSafeSegment` throw channel at 164 is invisible from the return type at 106. Row 1 needs an "and" (syntax + one existence check). |
| `resolveChatWorkspaceTarget` | 191-254 | **5.5** | 1, 2, 3, 4, 6, 7 | Row 2: 4 params → 8 returned fields, destructured at 2619-2627 and fanned out to ~40 sites. Row 4: doc omits the `'default'` exemption from the 404 (two exemptions, not one), the recovery gate's scope, and all 8 returned fields. Row 6: "authorized overrides body" is encoded three times (200-226, 302, 2760). |
| `resolveChatWorkspacePaths` | 263-342 | **4.5** | 1, 2, 3, 4, 6, 7 | Consumes two of the sibling's outputs (266-269) *that describe different workspaces*, and carries two admissibility checks (NOT_READY 304-310, VIEWER 311-319) that are not path knowledge. Row 4: doc omits the admissibility checks, omits `workspacePathFromTrustedConfig` entirely, and asserts a "never the home directory" guarantee the caller violates at 3010/3021 (TD-CHAT-26). |
| `buildChatCommandContext` | 350-402 | **7.75** | 2 (0.75), 6 (0.75), 7 (0.50) | Rows 2/7: a 7-field bag for 5 independent inputs — `executionWorkspaceId` and `effectiveWorkspace` are provably one value (224-226 → 2624 → 2872 → 2939, all `const`, no reassignment), and `persistedMemoryReadAllowed` is `allowsPersistedMemoryRead(turnMutationPolicy)`. Fixed by P4-06. Row 6: the deny gate is hidden but leaks as English (TD-CHAT-28). |
| `streamCannedReply` | 3227-3247 | **9.50** | 3 (1.00) partial | Genuinely deep: a 2-arg interface + boolean protocol hides 7 handler locals, SSE framing, the persistence rule and the zero-usage envelope. Needs **one comment line** (it never terminates the stream; the caller owns `raw.end()`), nothing else. |
| Handler viewer check | 2758-2765 | **3** | — | Dead behind `security-middleware.ts:895-903` except across a within-request `workspace.json` role change; a third copy of one rule. Phase 6 / founder. |
| `POST /api/chat` handler | 2561-5958 | **2.5** | 1, 2, 3, 4, 6, 7 | Rows 5 and 8 only. `hasCustomRunner` at 35 read sites; turn identity at ~157 sites under 13 names. TD-CHAT-3. |
| Cross-module (rows 3+6 only) | — | **4.0** | 3, 6 | `chat-persistence` exports a stateless probe, so the recovery gate is re-implemented per plugin; `chat-governance` exports "unavailable" as `undefined`, indistinguishable from "unrestricted". |

### 1.2 Helper set (the five module-level/handler-local helpers)

**≈6.9/10** (mean of 7.25, 5.5, 4.5, 7.75, 9.50). Two scouts published conflicting aggregates for overlapping sets — 8.2 (validator / command-context / canned cluster) and ≈8.1 (all five, scored relative to the 3397-line handler) vs 5.3 (the resolver pair, scored per-row against the diagnostics). **Use the per-module column in §1.1**; the 5.3 figure is the deeper analysis for the resolver pair and the 8.1 is a relative-to-the-handler reading, so the two are not comparable aggregates.

### 1.3 Eight diagnostics — helper set

| # | Row | Verdict | What it needs |
|---|---|---|---|
| 1 | Each module describable in one sentence | **FAIL** | The two resolvers need "ids AND history-layout gate" / "roots AND admissibility"; the validator needs "syntax AND one resource existence check". Fixed by the merge decision (§3) + P4-11/P4-12 comment rewrites. |
| 2 | Interfaces simpler than implementations | **FAIL** | Target 4→8; Paths 5→4 with two inputs being the sibling's outputs; command-context 7 fields for 5 inputs. P4-06 and P4-07 take the last one to 5. |
| 3 | Implementation changeable without touching callers | **FAIL** | `hasCustomRunner` 35 sites; turn identity ~157; `chat-persistence` status union re-read at 7 external sites; `chat-governance` return shape at 5 chat.ts sites + 2 agent modules; command-context sentinel text at 6 consumer equality checks. All ticketed, none apply-now. |
| 4 | Interface comments describe the abstraction | **FAIL** | Three false or incomplete doc blocks (94-96, 184-190, 256-262) + a false in-code claim at 3796. P4-11, P4-12 apply-now; 256-262 blocked on TD-CHAT-26. |
| 5 | Design discussion part of review | **PASS** | Phase 2/3 rows, the 51-agent Phase 3 review, this 90-vote panel. |
| 6 | Each module hides one important decision | **FAIL** | "Authorized overrides body" in 3 places; runner seam 35×; turn identity 157×; governance `undefined` conflates 5 facts; `PERSONAL_CHAT_SCOPE_ID` escapes as a filesystem key. |
| 7 | Newcomer understands boundaries without reading implementations | **FAIL** | `executionWorkspaceId` vs `effectiveWorkspace` indistinguishable from the signature (and the pin fixture builds production-impossible states); the validator's throw channel invisible; whether 311-319 can fire requires reading the middleware. |
| 8 | 10-20% of effort on design improvement | **PASS** | 8 PRs of design work in three days. |

**Passed 2 of 8.** Phase 4 pass 1 moves rows 2, 4 and 7 for `buildChatCommandContext` and the validator; rows 1, 3 and 6 are ticketed and gated on the P1 pin program.

---

## 2. Execution plan — commit order

Commit types: **T** test-only · **S** structure-only · **B** behavior-only · **D** docs-only. Structure and behavior never share a commit (Adopted Convention, TECH-DEBT.md:128). Every pin commit runs under Node 22.23.2.

### Wave 1 — docs (must precede anything that cites an id or reads the Map)

**P4-01 · D · `docs(tech-debt): refresh Phase 4 anchors and record the Phase 4 ledger rows`**
Touches: `docs/TECH-DEBT.md` Debt Ledger, Smell Inventory rows 74/86/87, `docs/REMOVE-TECHNICAL-DEBT-PLAN.md:91`.
- New rows TD-CHAT-30 … TD-CHAT-41, TD-TEST-8 (drafts in §5.2) and amendments to TD-CHAT-5/9/10/16/18/20/23/26/28 (§5.3).
- **Offset key** (replaces "≈+140"): Characterization Backlog ranges are at base `44baa77d`; the offset to HEAD is **+142 exactly for every range whose base start is ≥3299**, **+191/+192 for the two mid-handler rows (2805, 2880)**, **+339 for the pre-handler rows (1641, 1830, 1852, 2157; handler start 2222→2561)**. Rows 1547 and 1587 do not land at +339 — re-anchor by symbol. Row 2433-2443 is `[x]` and no longer maps by arithmetic (the code was extracted into `resolveChatWorkspacePaths`).
- **Also record why two refuters disagreed on four mappings**: the Backlog's printed numbers are coverage-derived *uncovered-statement starts*, 0-12 lines before the declaration (`runAgentAttempt` decl is 4804 at base, printed 4806; `runModelFallbackChain` 4970 vs 4967; `onHeld` opens 3590 vs 3591; TeamSync comment 4648 vs 4650; distillation banner 5254 vs 5266). Printed+142 and the HEAD declaration are both correct, about different lines. **Structural fix: re-anchor the Backlog by symbol**, quoting HEAD lines as a convenience only.
- `TurnScope` → **`ChatTurnScope`** in rows 74 and 86 and in PLAN.md:91. **Not** row 87 (that row says `ChatTurn`, not `TurnScope`). Rationale is *confusability* with the unrelated `WorkspaceTurnScope` class (`workspace-turn-coordinator.ts:258`, imported `chat.ts:463`, local `workspaceTurnScope` `chat.ts:2862`) — **not** an identifier collision; there is none.
- Census on row 86: **13 names, ≈167 read sites, 113 alias read lines** (`sessionId` 38 / `effectiveWorkspace` 45 / `sessionStateKey` 14 / `activeWorkspaceId` 11 / `activeExecutionWorkspaceId` 5), recorded as "read census, regex scan, 2026-09-16" with the reproducing command. Re-derive before writing — see §7.
- **Key Decision, recorded as agent-proposed pending founder ratification** (Rule 5 / PLAN.md:67, :72): *Rule 8 is line-based; a complete tsc-checked identity-alias inline is not exempt. Precedent `acd7ec0c` (breach, pinned after the fact in `a55a1712`) and `d242ec05` (13-line disclosure, pins in `9a9f96b7`) created no proof-of-equivalence carve-out.*

**P4-02 · D · `docs(testing): correct the Safety Net Map and extend the Characterization Backlog`**
Touches: `docs/TESTING.md` rows 54-60, Seam caveat (31-38), Characterization Backlog (77-101).

*Corrections (remove two false gaps — both would recommit the defect closed at `b5f18e6b`):*
- **Do NOT list `assertSafeSegment` traversal as a gap.** It is pinned: `tests/local/phase2-traversal-chat.test.ts:81-135` (`workspace: '../evil'` → 400 + nothing written outside root; `session: '../evil'`; encoded `..%2f..%2fevil`; valid-segment 200), and that file is line 125 of the 16-file CI set. Add it to row 54's **Pinned behaviors** and **Test files**.
- **Do NOT list `MESSAGE_TOO_LONG` as a gap.** Pinned at `tests/behaviors/chat-pipeline.test.ts:335-348` against a real `buildLocalServer`, inside the default `npm run test` gate. Add to row 54 Pinned behaviors, flagged "outside the 16-file chat.ts coverage set". Listing it as a gap would freeze 122-125 — the exact lines TD-CHAT-5's fix must touch.

*Row 54 additions:* `message` missing/empty → 400 `message is required` (`sse-resilience.test.ts:156` exact; `chat-api.test.ts:2347-2365` status/`toContain` only — TD-TEST-7); the `selectedSkill` trio 409/400/400 (`persona-acceptance-prompt-budget.test.ts:536-558`, assertions 555-557) plus the success pin 512-535. Test-files column becomes: `chat-route-characterization.test.ts`, `persona-acceptance-prompt-budget.test.ts`, `phase2-traversal-chat.test.ts`, `chat-api.test.ts`, `sse-resilience.test.ts`, `tests/behaviors/chat-pipeline.test.ts`.
*Row 54 Gaps* (keeping `SESSION_ID_CONFLICT`): `message` non-string → 400 `INVALID_FIELD_TYPE` (chat.ts:119-121, **zero hits repo-wide**); `selectedSkill` normalization `trim().toLowerCase()` (chat.ts:**131**, not 132 — 132 is the grammar regex, already pinned at :539); the skill-409-vs-later-400 exit order under combined invalidity.
*Row 55 Gaps:* re-anchor `chat.ts:303-311 at b248ce38` → **`311-319` at HEAD**; keep the entry (it is TD-CHAT-18's precondition); note the middleware emits a *different* `error` string.
*Row 56 Gaps:* add canned-reply allow-history persistence (3237-3240), abort-before-`done` (3232/3236 → caller early return at 3288/3293), per-word framing + trailing space (3233 — *presence* of `event: token` is already pinned at `sse-resilience.test.ts:92`), `token`-before-`done` ordering, and the deny-branch `persistMessage` suppression (the persona pin at 4650-**4707** observes `capturedConfig.messages`, i.e. in-memory `history` only; the disk write at 3239 is unobserved in both polarities). Narrow the stale "reroute with an available model" gap to *the `Processing /… via AI` step event only* — `chat-api.test.ts:4017` already executes the reroute.
*Row 57 Gaps:* `listSkills` closes with P4-05.
*Row 58 / Backlog:* echo-path allow-history persistence (3303-3309 → 3237-3240) and echo-path abort (3308).
*New Map rows* (all currently absent, which blocks Rule-8 reasoning about them): `routes/agent.ts`, `routes/backup.ts`, `routes/chat-persistence.ts`, `routes/artifact-index.ts` + `routes/artifacts.ts`, `routes/commands.ts` + `routes/command.ts`, and the agent-package modules `agent-loop.ts` / `tool-executor.ts` / `subagent-orchestrator.ts` / `subagent-tools.ts` / `chat-collaboration.ts`.
*New Backlog entries:* `4046-4060` team governance lookup (P1 — security fail-open); `4737-4771` generated-artifact indexing (P2); `5755-5786` failure-tail cost accounting (P2); `3555-3562` `approval_auto` audit payload (P1); and — under a **new subheading** "Unpinned single-emission sites (read census, not coverage)", because the Backlog's own rule is "≥8 lines, statement coverage" — `3317-3318` `agent:started`, `3320-3329` budget warn (`Budget limit reached`, the observable is the `sendEvent('step')` at **3326**), `3337-3342` activation fallback (observable at **3340**), `5670-5690` completion notification (`finished in`, incl. the swallowing catch). Do **not** add `3756`/`3760` — they are inside the existing `3591-3619` row.
*Seam caveat:* the "8 tests in `chat-api.test.ts`" figure is stale and contested — **re-measure and state the method** (§7).

### Wave 2 — the P1 pin program (test-only; unblocks everything else)

Ordered by the ledger's own priorities, not the scouts'. **Assertion-surface constraint (founder ratification F15):** every pin in this wave asserts HTTP status, SSE event sequence/body, the persisted transcript, or audit/cron store rows read back through a public reader. **No pin asserts `emitAuditEvent` call arguments by field name, and no pin names a handler local** (`sessionId`, `effectiveWorkspace`, `sessionStateKey`, `activeWorkspaceId`, `activeExecutionWorkspaceId`, `executionScopeId`). Rationale: those seven values are one decision at ~157 sites; white-box pins would fossilise exactly the leak TD-CHAT-3 exists to remove — the way the `buildChatCommandContext` pins already fossilised a two-field leak (TD-TEST-8).

**P4-03 · T · workspace chat-runtime creation failure** — `chat.ts:2949-2998` (Backlog `2805-2826`, P1).
Seam is **fault injection, not a fetch spy**: the throw at 2997 precedes every provider call, so no fetch stub is reachable. Leave `server.agentRunner` unset (2931 false), use a managed named workspace, and stub `server.agentState.getWorkspaceMindDb` → null (`local/index.ts:1815-1818`), restored per Convention 146. Request `workspace` must equal the authorized workspace or `chat.ts:304-310` pre-empts with 409 `WORKSPACE_NOT_READY`. Assert: 200 + SSE `error` carrying `Workspace "<id>" is not ready for chat.` verbatim, **plus two absence pins** — no `GENERATION FAILED` assistant turn (5859's guard is false because `activeHistory` is only assigned at 3112) and no raw-turn memory capture. Note in the commit that 2951-2993 is already traversed-but-unasserted by the chat-api fetch-spy tests.

**P4-04 · T · retry / model-fallback / credential-rotation chain** — HEAD 4946 (`runAgentAttempt`), 5112 (`runModelFallbackChain`), 5170 (`runPrimaryWithSafeInterruptedRetry`), 5213, 5258. Backlog `4806-5146`, P1. **This is TD-CHAT-10's own recorded prerequisite** ("characterize via fetch-spy harness before any Phase 2 extraction touches the attempt loop") and both scouts omitted it. Fetch-spy harness.

**P4-05 · T · compression-summary injection scan + command-context gaps** — `chat.ts:3986-4025` (Backlog `3863-3885`, P1, injection defense), plus the two cheap helper-set pins that unblock Wave 3: a **`listSkills` direct pin** in `chat-command-context-characterization.test.ts` (closes row-57 Gaps; requires giving `stubServer` an optional `skills` argument, default `[]`), and the two validator pins from row-54 Gaps (`message: 42` → 400 `INVALID_FIELD_TYPE`; `selectedSkill: '  Risk-Assessment  '` → 200 with the normalized name reaching the tool schema — that one must live in `persona-acceptance-prompt-budget.test.ts` beside 512-535, since the `post` helper at `chat-route-characterization.test.ts:41-46` asserts the runner is *not* reached).

**P4-06 · T · approval hook** — the whole pre-tool hook in one harness (fetch-spy returning a tool_call): `proposeHeldTurn` 3574-3600 (Backlog `3432-3458`), Always-allow grant 3611-3626 (`3469-3484`), install_capability trust assessment 3641-3674 (`3499-3532`), `approval_requested` 3688, `approval_held` 3736 + the `onHeld` closure 3732-3744, and the three audit emissions `approval_auto` 3555-3562 / `approval_denied` 3756 / `approval_granted` 3760 (three separate triggers, three tests). **Record in TESTING.md that `chat-approval-timeout.test.ts` is a direct unit of the exported `waitForApprovalDecision` and covers *none* of these handler lines** — Map row 58 currently implies otherwise. Same for `chat-governance.test.ts` vs `chat.ts:4046-4068`.

**P4-07 · T · post-commit observer + closed-DB** — split into two, they are different behaviors and only one needs a fetch spy: (a) `chat.ts:5710-5720` `isClosedDbError` inside the auto-save catch (fetch-spy; every post-commit block is `!hasCustomRunner`-gated); (b) `chat.ts:5724-5733` post-commit observer — reachable with an injected `agentRunner` by making `server.agentState.listWorkspaces` (read unguarded at 5673-5674) throw. Backlog `5570-5591`, P2.

**P4-08 · T · TeamSync push** — `chat.ts:4790-4822` (Backlog `4650-4682`, P2). Real cost: a `teamId` workspace + a bound team-server token + a URL that survives `fetchTeamServer` → `safeFetch`'s `assertUrlAllowed` and the https/allowLocal check *before* `globalThis.fetch` is consulted; `sync.pushFrame` is fire-and-forget with a `.catch`, so poll rather than asserting after `done`. Pin `gop_id === sessionId` and mark the dead `?? 'unknown'` guard `// QUIRK (TD-CHAT-40)`.

**P4-09 · T · turn-trace emission** — new `startTurnCapture`/`stopTurnCapture` pins (exported from `@waggle/agent`, `packages/agent/src/index.ts:5-6`; no `packages/server` test imports them today). Pin today's asymmetry: Target-class rejections (404, recovery-409) emit **zero** `chat.turn.start`; the four post-log pre-hijack exits (Paths rejections 2662, `INJECTION_DETECTED` 2742, handler viewer 403 2761, `PATH_TRAVERSAL` 2777) each emit **exactly one** with no downstream stage event. `stopTurnCapture()` in `afterEach` — the buffer is module-global. Mark `// QUIRK (TD-CHAT-31)`.

**P4-10 · T · TD-CHAT-23 pin set** — §4.

**P4-10b · T · sibling command routes + artifact index** — value pins on `commands.ts:67,71,83` / `command.ts:259,263,274` (today `tests/routes/commands.test.ts` exercises them but asserts only headings — `'Catch-Up Briefing'`, `'Memory Search'` — which every branch emits); an agent-package contract pin in `packages/agent/tests/workflow-commands.test.ts` (its stub at :10-11 returns non-sentinel strings, so all six equality branches are untaken-false today, and `workflow-commands.ts:218` is unreachable from a chat turn so the agent package is its only possible pin site); and the TD-CHAT-34 artifact pin via the **`agentRunner` `onToolResult` seam** (there is no `hasCustomRunner` gate between 4666 and 4771 — prior art `persona-acceptance-prompt-budget.test.ts:1307`). Cross-check first: `smart-router-chat.test.ts:117-211` may already pin the *managed-workspace* artifact path (both the `patch` and `add` branches) and `:113` the `spendWorkspaceId` — if so, those two drop out of TD-CHAT-34's gap list and into the Map.

### Wave 3 — structure-only commits

**P4-11 · S · `refactor(server): collapse the duplicate workspace-id input of buildChatCommandContext`**
*(merges the duplicate proposals P4-CCC-1 — SURVIVES 0/3 — and P4-A3 — refuted 3/3 on citations only; the union of corrections applies.)*
- Touches: `chat.ts:344-349` (comment), `350-358` (−1 field: delete 355), `360` only (361 carries the other two names), **385 and 388** (not "384-387" — 384 is a closing brace, 386-387 are the `buildWorkspaceNowBlock` header), call site **3259 only** (not "3257-3259" — 3257 is `executionWorkspaceId` and must survive).
- **Do not reflow the object literal at 386-391** — 389-390 (`activateWorkspaceMind`, `cronSchedules`) are the row-57 Gaps entry.
- Pin file: **delete line 79** (77 already carries `executionWorkspaceId: undefined`); rename the **six** override sites 163, 176, 182, 191, 197, 209. **Line 89 already reads `executionWorkspaceId` and must not be touched.** Seven edits total, zero assertion changes.
- **Mandatory gate:** `npx tsc --noEmit` over a config that *includes* the pin file, not just `packages/server/tsconfig.json`. A missed rename fails open — 163/176/182 return at the memory/history gate before 385, and 191/197 expect exactly the no-workspace fallback string, so **five of six** would pass vacuously; only 209 goes red.
- Disclose in the body: (i) the equivalence rests on the const chain 224-226 → 2624 → 2872 → 2939 with no reassignment — tsc + proof, not a test; (ii) no route pin runs a slash command in a *named* workspace (all six route pins at `chat-route-characterization.test.ts:137-196` are personal turns), so the non-undefined branch is carried by the direct pins; (iii) Rule 8 shared-line note for `server`/`listSkills`; (iv) Convention 142 exempts option-bag fields "until their producer is touched" — this touches the producer, so the collapse is the conventional outcome.
- Keep the name `executionWorkspaceId`. Doc addition, one sentence: *"`workspaceId` is the execution workspace id, or `PERSONAL_CHAT_COMMAND_CONTEXT` when the turn has no execution workspace."*
- Follow-up D commit: Smell Inventory row 75 records the 7-field signature and goes stale; record TD-TEST-8 (the retired implicit pin: after the collapse, `workspaceId` evaluates differently in six tests with no assertion observing it).

**P4-12 · S · `refactor(server): derive persistedMemoryReadAllowed inside buildChatCommandContext`** *(after P4-05's `listSkills` pin — 397 is a re-bound reader and `listSkills` is a row-57 Gaps entry.)*
- Delete field 356 and the name from destructure 361; add `const persistedMemoryReadAllowed = allowsPersistedMemoryRead(turnMutationPolicy);` after the destructure, before `return {` at 363; delete call-site 3260. Import already present at 422. The handler const at 2700 stays (8 remaining uses: 3267, 3270, 3507, 3812, 3869, 3898, 4414, 4446 — not "14").
- **Pin handling (the panel's one genuine disagreement, resolved):** the "persisted denial wins over history denial" meaning of test:180-184 comes today from *two independently forced* denials (a history-directive message + an explicit `persistedMemoryReadAllowed: false` override). One refuter judged the re-expression vacuous; another established that precedence is **already pinned independently** at test:167-178 (`/now - use only the supplied evidence` → `contextScope: 'supplied-only'` → both gates deny). **Do both:** land the replacement as a *separate pin commit* before the structure commit, re-expressing 180-184 as a deny-read-only message **and** adding the explicit policy assertions the sibling pins at 158-166/168-177 already use — `expect(allowsPersistedMemoryRead(policy)).toBe(false)` and `expect(allowsConversationHistory(policy)).toBe(...)`. Without those two lines the pin is vacuous (381 short-circuits before 382) and would be a silent coverage regression.
- Probe the chosen message before writing. If no directive yields both denials, the row becomes **no-change** and the two-encodings smell is ledgered instead.
- Ledger the interface-direction tension: after this, the helper's interface is policy-typed, so a future single producer shared with `routes/commands.ts:60-89` (no `TurnMutationPolicy` in scope) must synthesise one. The competing one-field reduction is booleans-only (`persistedMemoryReadAllowed` + `conversationHistoryAllowed`, dropping `turnMutationPolicy`, whose only other use is 382). Record, do not decide silently.

**P4-13 · S · `refactor(server): rewrite the validateChatRequestFields interface comment`**
- Range is **94-97**, not 94-96: line 97's "Unsafe path segments **still** throw" is anaphoric to the sentence being replaced and dangles otherwise. Keep 98-101 verbatim.
- Replacement states the **source order** explicitly so the 409's position is unambiguous — `message` (required, string, length) → `selectedSkill` (string, grammar, then 409 `SKILL_NOT_AVAILABLE` when not installed) → `retry` → `retryTarget` (shape, then the `retry: true` coupling) → the four segments — *so an uninstalled skill outranks a later malformed `retry`*. Returns the trimmed, lower-cased `selectedSkill` and the parsed `retryTarget` (`null` when absent).
- **Delete the false clauses, do not replace them with new ones.** "before any referenced resource is resolved" is contradicted by 135-137. **Do not write "workspace and session existence are left to the resolvers that follow"** — there is no session-existence resolver anywhere in chat.ts (`SESSION_NOT_FOUND` has zero occurrences). Say instead: workspace existence is left to `resolveChatWorkspaceTarget` (404 `WORKSPACE_NOT_FOUND`); `session`/`sessionId` disagreement is rejected by the caller (`SESSION_ID_CONFLICT`).
- **Amend `chat.ts:2578` in the same commit** — `// Reject malformed request fields before resolving referenced resources.` carries the identical retracted claim four lines above the call site. Keep 2579-2581 verbatim (still true).
- Symbol locators, not line numbers (Convention 144).

**P4-14 · S · `refactor(server): rewrite the resolveChatWorkspaceTarget interface comment`** *(the Target half of P4-WS-8 only)*
- Touches `chat.ts:184-190` only. Add: **both** 404 exemptions (`historyWorkspace` falsy → personal scope; the literal `'default'` at 208); that the 404 is decided against `historyWorkspaceConfig` (203-205, the **history** id) while the *returned* `workspaceConfig` (197-199) is the **body** id's config — the single highest-value sentence in the whole comment pass; that the recovery gate fires whenever the resolved history id is `'default'`, i.e. an explicit default **and every personal-scope turn**; one clause per returned field naming which workspace it describes; and that `WorkspaceManager.get` re-reads `workspace.json` per call, so all four configs are value-equal snapshots, never shared references.
- **Do not touch `chat.ts:256-262`** (the Paths doc) — it is a TD-CHAT-26 locator whose recorded status is "founder decision required **before any edit**", and it contains the "never the user's home directory" clause that is the row's evidence.
- **Do not strip `Review Critical #3:` at 2758-2759.** That is a sweep under Convention 144 (no pinned change lands at 2760 in this commit), and the sentence it heads is the only in-source statement of the invariant that every rejection must precede `reply.hijack()` at 2787. Stripping the `A2:` lead at 2652 is in scope only if the same commit touches the call at 2654.
- Do **not** write "the viewer 403 duplicates the middleware gate as defense in depth" — that resolves open TD-CHAT-9.

**P4-15 · S · `refactor(server): document that streamCannedReply does not terminate the stream`**
One line after `chat.ts:3229`: the helper never terminates the stream; the caller owns `raw.end()` — 3289 and 3294 call it, 3308 deliberately does not and falls through the skipped agent-loop block to the outer `finally` at **5907** with `raw.end()` at **5951** (not "5731/5820", which are inside the `catch` at 5724). Comment-only; upgrades P4-SCR-1 from neutral to marginally lowering.

**P4-16 · S · `refactor(server): export resolveChatWorkspacePaths for direct pins`** — **conditional.**
One token at `chat.ts:263`. **Try the no-export route first:** `vi.spyOn(server.workspaceManager, 'get')` delegating to the original for the Target calls (197/203/228) and returning a `{ teamId, teamRole: 'viewer' }` config for the Paths call (303) reaches 311-319 through the real route and the real middleware, restored in `finally` per Convention 146 (`get` re-reads `workspace.json` per call, `workspace-manager.ts:307-323`, and `update()` exists, so the TOCTOU window is genuinely drivable). Only if that is demonstrably infeasible does the export land, as its own commit mirroring `80006984`, gated on `npx tsc --noEmit --project packages/server/tsconfig.json` (that project sets `declaration: true` + `composite: true` and the helper has an inferred return type).
**Do not export `resolveChatWorkspaceTarget`** — every one of its branches is already route-pinned; the export buys no pin and widens the interface.

**P4-17 · T · the 403 `VIEWER_READ_ONLY` pin** (after P4-16).
**The pin must assert the exact `error` string**, not just the code: `'Viewers cannot send messages in team workspaces. Ask a team admin to upgrade your role.'` (chat.ts:315) vs the middleware's `'Viewers cannot modify team workspaces. …'` (security-middleware.ts:899). Same `code`, different message — without the string assertion the pin cannot tell which layer answered, and `chat-api.test.ts:4062-4090` (which asserts status + code only) would pass with either gate deleted. Mark `// DEAD-IN-PRODUCTION (TD-CHAT-9 / TD-CHAT-18)` so the pin does not later become an argument against Phase 6 deletion. Then: move the entry out of row-55 Gaps and record in TD-CHAT-18 that its precondition is met.

### Wave 4 — behavior commits (each alone, each with a deliberate re-pin)

**P4-18 · B · TD-CHAT-23 governance half** — §4.
**P4-19 · B · TD-CHAT-23 trustMeta half** — §4. **Blocked** on F2.
**P4-20 · B · install_capability first-catch log** (`chat.ts:3641-3657`, catch at 3656) — the fall-through is *not* fail-open (the heuristic block supplies `approvalClass`), so this is a missing-log fix under Convention 138 bullet 1, with its own pin (`install_capability` with `name: 123` reaches it in production via `path.basename` `ERR_INVALID_ARG_TYPE`). Never mixed with P4-19.

---

## 3. Refuted — no-change or ledger row, with the decisive reason

| Proposal | Decisive reason | Disposition |
|---|---|---|
| **P4-WS-4** move `chat.turn.start` below the Paths call | The premise is false: `logTurnEvent` is called **once** in chat.ts (2645) and no `chat.turn.end` stage exists anywhere (the terminal stage is `agent-loop.exit`), so "dangling start" is the normal shape for every command turn, every custom-runner turn and every post-hijack failure. The move also fixes only 1 of 4 exits (2742, 2761, 2777 still dangle) and **reverses a ratified design decision** — `docs/plans/H-AUDIT-1-DESIGN-DOC-2026-04-22.md:27,34` records the generation point as "orchestrator turn entry / POST /api/chat entry", restated in-code at 2638-2643 and `turn-context.ts:4-5`. | **No-change.** Ledger TD-CHAT-31; pin the current emission (P4-09). The preferred alternative (log **above** both resolvers, at true entry) is founder decision **F4** and changes the `workspace:` field, so it is a behavior commit either way. |
| **P4-WS-5** merge both resolvers into one helper | Cannot be structure-only: the log at 2644-2650 reads `executionWorkspaceId`, a Target output, so the merged call is forced to 2610 and four Paths rejections stop emitting `chat.turn.start`. The stated body ranges also drop load-bearing statements (274 — the only path by which `explicitWorkspacePath` reaches `workspacePath` with no body workspace; 301 — seeds `executionWorkspacePath`; 340-341), and the "10-field `const` destructure stays as-is" does not compile (2782 reassigns `workspacePath`; no such destructure exists at HEAD). Interface widens 8+4 → 11 fields; diagnostics 1 and 6 both fail for the merged unit (3 decisions, 6 rejections across 403/404/409). | **No-change for Phase 4.** Record as a Phase 6 founder decision (**F5**) with the blockers named: P4-09/F4, P4-17, and TD-CHAT-18 sequencing. |
| **P4-WS-6** move NOT_READY + 403 into Target | The stated insertion point ("directly after 228-230") places the moved block **before** the pinned recovery-409 gate at 231-243, and the two are co-satisfiable in exactly the shape the one live NOT_READY pin uses (`activeWorkspaceId = 'default'`, no default config, `recovery-required` layout) — it swaps one pinned 409 code for another. The proposed bag also cannot reproduce the 302 predicate (`authorizedWorkspace === undefined && workspace === undefined` yields `executionWorkspaceId === 'default' !== workspace`). | **No-change for Phase 4.** Record the corrected shape for F5: insert after **243**, carry the `if (authorizedWorkspace && …)` guard verbatim, and add an explicit `isExecutionWorkspaceOverridden` boolean to Target's return rather than re-deriving. |
| **P4-WS-7** keep both viewer sites | *(Not refuted in substance — 1 of 3.)* The 403 range is **311-319**, not 310-318; the cited pin does not distinguish the layers; and "untestable" is false. | **No-change decision stands**, corrected. Feeds P4-17 and TD-CHAT-9's amendment. |
| **P4-WS-8b** Paths interface comment | `chat.ts:256-262` is a TD-CHAT-26 locator whose status is "founder decision on the personal-scope root required **before any edit**"; the proposed text also contained two new false clauses and omitted the third returned field. | **Blocked** on **F7**. P4-14 lands the Target half. |
| **P4-VAL-2** validator takes `request.body` | As specified (`Record<string, unknown>`) it converts a compile error into a silent runtime skip of the `assertSafeSegment` traversal guard (a mistyped key yields `undefined`, and 157 `continue`s), retires the anchor of ratified Convention 142, orphans `selectedSkillRaw`/`retryTargetRaw` with nothing in the toolchain to flag them, and collides with TD-CHAT-18 on the same hunk. Two of three refuters independently recommend deferral. | **Defer.** Record the key-checked variant (extract `ChatRequestBody`, `type ChatRequestFieldInput = Partial<Record<ValidatedChatField, unknown>>`) for after TD-CHAT-18. Its useful half — the row-54 Map corrections and the two new validator pins — lands in P4-02/P4-05. |
| **P4-A1** `ChatTurnScope` inside `resolveChatWorkspaceTarget` | The proposed destructure drops `usesNamedWorkspace` (read at 2658 and six sites below); `executionWorkspaceId` is read at **2647**, above the claimed rebinding window; "no read site below 2878 changes" is false unless three new snapshot consts are added, and `const activeSessionId = scope.sessionId` is precisely the destructure-rename form Convention 142 forbids; the helper is not exported, so step 1 cannot compile; and the shape inverts the ratified Smell row 74 ("Introduce Parameter Object **once the method object exists**"). | **Defer to Phase 4 pass 2 / TD-CHAT-3**, gated on the P1 pin program and **F12**/**F13**. Its prerequisite id-derivation pins are Rule-8-free and may land now as a test-only commit. When it lands: compose `readonly history: ChatHistoryTarget` rather than re-spelling its four fields; rename the function to `resolveChatTurnScope` and rewrite its doc; drop the false invariant "callers never recombine" (the DELETE handler at 5970-6014 re-derives the same ids independently). |
| **P4-A2** Inline Variable ×5 standalone | *(Not refuted — 0/3; it is itself a defer.)* But its justification must be corrected: **tsc is not a complete oracle.** Three read sites are ES6 shorthand into untyped or index-signature sinks where a renamed key compiles clean and changes an observable payload — `chat.ts:5447` and `5715` (`log.warn(msg, data?: unknown)`, JSON-stringified into the line) and `chat.ts:4897` (`WaggleMessage.content`, `Record<string, unknown>`, broadcast on the SignalBus; the only chat-side pin asserts `subtype`/`teamId` only). | **Defer; subsumed by the TD-CHAT-3 method object.** Add three sink-level pins (5443-5450, 5711-5718, 4887-4900) and a mandatory precondition: expand every shorthand naming an inlined identifier to explicit `key: value` **first**, as its own structure-only step verified by `git diff -w`. Remove 3318 from its footprint. |
| **P4-X7** split `executionScopeId` into two fields | The split already exists (224 vs 227, two names, two types, both returned and both destructured); the "must pick the former" forcing mechanism is false (two `string`-typed fields enforce nothing); the proposed name `observabilityScopeId` misdescribes four of the nine consumers (MCP tool pool 3870, spend budget 4188/4579, collaboration binding 4197, filesystem path 4757-4767); and repointing the artifact consumer is a behavior change, not a type refactor. | **No-change.** The fan-out table (four roles, not two) folds into the Alias Chain row; the filesystem role is TD-CHAT-34. **Pin-attribution correction for the ledger:** `chat-api.test.ts:3997-4003` pins **5379** (the success-path `addUsage` inside `if (hasCustomRunner)`), not 5776 (the failure tail inside the catch at 5724). 5776's personal-sentinel branch is unpinned. |
| **P4-X5** wire-code leaf module | The premise is false — Convention 140 is scoped to codes *emitted by chat.ts*, so no sibling is obliged to share the map; the proposed remedy (`routes/chat-error-codes.ts`) sits under `routes/` and does not change the import direction from `local/security-middleware.ts`; and a constants-only module is classitis (hides no decision, interface = implementation). A no-new-module path exists: `security-middleware.ts` imports no routes, so chat.ts may import from it. | **No-change; C3 stands as ratified.** The rest of the claim survives as ledger content: delete the false gap (`CHAT_TURN_IN_PROGRESS` **is** pinned at `backup-restore.test.ts:482-484`) and replace the synonym row with a **scope table + explicit do-not-unify guard** (§5.2, TD-CHAT-36). |
| **P4-X1** chat-history recovery gate | `isolateLegacyDefaultChatSessions` is **not a pure probe** — it renames `workspaces/default/sessions` and writes the layout marker, so probe *count* is observable and already pinned (`chat-api.test.ts:4697` vs `:4712`). Routing `chat.ts:1871-1873` through `current()` would turn one registration probe into two, delete the one-time `log.warn`, and move the rename + marker write to server boot. A boolean `needsRecovery` cannot supply `.code`/`.reason`. Folding `backup.ts` into a registration-cached gate is a **fail-open behavior change** on the one route whose job is to mutate the dataDir. | **Defer to Phase 6**, with the corrected interface recorded: one method `statusFor(historyWorkspaceId): ChatHistoryLayoutStatus`; `reset()` re-probes **eagerly** (mirroring 2175-2177); the warn stays in chat.ts; `backup.ts` keeps its per-request probe. Blocked on the new Map rows in P4-02. |
| **P4-X6** total `GovernancePolicies` | Core direction sound (1 of 3 refuters upheld it). Blocked as scoped: the `source` discriminant has **no consumer** (the only decision reading it is deferred), re-exports the helper's internal TTL cache into its public contract, and breaks seven exact-match `toEqual` assertions the claim never named. "Every chat.ts site is a `!hasCustomRunner` Backlog region" is false (4046-4060 and 4592 are unguarded and reach the production `agentRunner` seam). And `chat.ts:4049-4059` is **not dead** — see §4. | **Defer to Phase 5/6**, `source` dropped, `blockedTools: readonly string[]` only. Immediate output is the Backlog entry + ledger cross-ref in P4-01/P4-02. |
| **P4-B1** TD-CHAT-16 reaffirm | *(Not refuted in substance.)* Counts wrong: **27** `!hasCustomRunner` gates (29 grep hits minus 666 in the helper and 3796 in a comment), **8** positive reads (2935, 3197, 3515, 3768, 4339, 4443, 5373, 5770), 35 handler reads total. The "fetch-spy harness cannot replace them" blocker is **false** — the `runAgentLoop` module mock (`persona-acceptance-prompt-budget.test.ts:46-51`) leaves `agentRunner` unset, so all 27 gates execute on 72 route posts in that file alone. `shouldPackageSystemPromptForTurn` (661-667) is a shallow helper and the wrong exemplar. | **Docs-only**, folded into P4-01. Real blocker restated: the 119 `agentRunner =` assignment **lines** assert on the injected object itself, so each site must be rewritten against the mock's `config`. |
| **P4-P1** alias-region pins | Priority inversion (only 2949-2998 is P1); omits the two ledger-named mandatory P1s (TD-CHAT-10's retry chain, the injection scan); harness prescription wrong in both directions; `chat.ts:4810` is not pinnable (dead guard); no assertion-surface constraint. | **Superseded by Wave 2**, reordered and re-seamed. |
| **P4-LED-1** / **P4-L1** / **P4-X4** / **P4-X2** / **P4-X3** / **P4-VAL-3** / **P4-SCR-1** | Corrections only. | **Folded into P4-01 / P4-02 / §5.** |

---

## 4. TD-CHAT-23 execution plan

One ledger row, two independent catches, two different reachability stories. **Order: governance pin → governance fix → trust re-verification → trust pin → trust fix.** They never share a commit.

### 4.0 The blocking dispute (read first)

The trust-pin scout's load-bearing premise — `catch` at `chat.ts:3673` is **unreachable in production** — is **contradicted by one refuter with a specific, checkable chain** and **accepted without independent test by the other two**. The counter-proof:

`chat.ts:4210-4213` passes `securityContext: { hooks: requestHookRegistry }` → `chat-collaboration.ts:718`/`:741` pass `hooks: securityContext.hooks` into `createSubAgentTools`/`createWorkflowTools` → `subagent-tools.ts:429` uses them for the child `runLoop`. Child loops do **not** run `chat.ts:4649`'s `describeToolUse`: `chat-collaboration.ts:720` defines `onSubAgentTool` as `(runId, name) => …` and never touches `input`, and `createWorkflowTools` is passed no `onToolUse` at all, so `tool-executor.ts:126` is skipped. For `bash` (`riskLevel: 'high'`, `system-tools.ts:596-600`, surfaced as `ctx.riskLevel` at `tool-executor.ts:161-165`), every earlier coercion short-circuits at `confirmation.ts:284` (`if (trustedRiskLevel && riskAtLeast(trustedRiskLevel,'high')) return true;`) **before** `String(args?.command)` at 297 — but `classifyGatedToolRisk` calls `isCriticalNeverAutopass(toolName, args)` **without** `trustedRiskLevel` (`confirmation.ts:203`), so 296-297 runs and a JSON-producible `{"toString":0}` throws.

**If that chain holds**, three things change: the pin must use the production stimulus (a child `bash` call), not a `vi.mock` sentinel on `write_file`; the observable today is an **orphan approval card** (`approval_required` + `approval_requested` audit, then `chat.ts:3697` throws unguarded, `hooks.ts:88-94` swallows, `tool-executor.ts:184-204` emits the critical `[BLOCKED]`; `POST /api/approval/:id` 404s) rather than the held-row outcome the scout described; and the fix **converts an always-denied critical `bash` into an approvable one**. That is a security-posture change requiring explicit founder sign-off (**F2**), not an incidental side effect.

**Action: re-verify the chain before P4-19 is written.** Do not schedule the trust-half fix on the strength of either reading.

### 4.1 Governance half (`chat.ts:4046-4060`) — verified by all three lenses

**Facts confirmed at HEAD.** The block is **not** `hasCustomRunner`-gated (AST-verified: `chat.ts:4048` and `wsConfig` at 3799 are sibling children of the `if (shouldRunAgentLoop)` block at 3311; the first `!hasCustomRunner` after it is 4062). Four throw paths escape the helper's own `try` (45-61): `new WaggleConfig(dataDir)` at 32 (`config.ts:202-213` `mkdirSync`, EPERM/EACCES on the Windows Solo target; `load()` at 215-241 rethrows non-`InvalidConfigError` and rethrows `primaryError` with no valid `.bak`), `getTeamServer()` at 33, `extractRolePolicy(cached.permissions, …)` at **42** on the cache-hit path, and again at **59** inside the catch. `policyCache.set` at 55 precedes `extractRolePolicy` at 56, so a wire array containing `null` is cached before it is validated and `permissions.find(p => p.role === …)` at 70-71 throws `TypeError` for the whole 5-minute TTL. Consequence today: `chat.ts:4056` swallows → `governancePolicies` undefined → 4065 filters nothing → `agent-loop.ts:1782` gets `blockedTools: undefined` → every governance-blocked tool runs.

**P4-10a · T · governance boundary pins** — new `describe` in `packages/server/tests/local/chat-route-characterization.test.ts` (**not** `chat-governance.test.ts`, whose `vi.mock('@waggle/core')` at 20-25 replaces `WaggleConfig` for `buildLocalServer`).

*Fixture:* `mkdtempSync` dataDir → `buildLocalServer({ dataDir })` → `server.agentRunner = async (config) => { captured.push(config); config.onToken?.('ok'); return { content: 'ok', toolsUsed: [], usage: { inputTokens: 1, outputTokens: 1 } }; }`. `new WaggleConfig(dataDir).setTeamServer({ url: 'https://93.184.216.34', token: 'gov-char-token' }); save();` — `TeamServerConfig` has no `teamSlug`, so the target is `/api/teams/default/capability-policies`. `resetRateLimiter(server)` (`tests/test-utils.ts:44`) in `beforeEach`; `clearTeamServer(); save();` + close + `rmSync` in `afterAll`. Workspace: `server.workspaceManager.create({ name: \`gov char ${label} ${Date.now()}\`, group: 'test', teamId: …, teamRole: 'member' })` — `CreateWorkspaceOptions.teamId`/`.teamRole` are at `packages/hive-mind-core/src/workspace-manager.ts:97`/`:99`, applied in `create()` at :249/:251; precedent `packages/server/tests/chat-api.test.ts:3680-3691`. **Every POST goes through `injectWithAuth`** (imported at `chat-route-characterization.test.ts:16`); a raw `server.inject` 401s before reaching 4048 and produces a false "swallow" pin. `vi.spyOn(globalThis, 'fetch')` reaches the call through `fetchTeamServer` → `safeFetch` (`url-egress-guard.ts:482`); an https IP literal skips DNS and needs no `WAGGLE_ALLOW_LOCAL_FETCH`. **Every governance workspace name carries a `Date.now()` nonce** — `policyCache` is module-level, keyed by the slugged workspace id, with no exported reset.

*Pin G1 (seam proof, unchanged by the fix):* payload `[{role:'admin',blockedTools:['delete_workspace']},{role:'member',blockedTools:['bash']}]`; assert 200, a `done` event, `captured.at(-1)!.governancePolicies` `toEqual({ blockedTools: ['bash'] })`, `captured.at(-1)!.tools` `toEqual([])`, and **URL-filtered** fetch count `=== 1` with `Authorization: Bearer gov-char-token`. *(Optional — 4046-4055 is already pinned at `persona-acceptance-prompt-budget.test.ts:1181-1247`.)*

*Pin G2 (the swallow; flips after the fix) — required:* fresh workspace, payload `[null, {role:'member',blockedTools:['bash']}]`, two turns inside the TTL. Today: both 200, both carry `done` and no `error`; `captured.length` +2; **both** `governancePolicies === undefined`; URL-filtered fetch count **exactly 1** (the turn-2 cache hit proves the throw came from line 42, not a refetch). Mark turn 1 `// QUIRK (TD-CHAT-30)` and turn 2 `// QUIRK (TD-CHAT-23)`.

*Pin G3 (the unpinned territory the fix newly reaches) — required, and missing from both scouts:* the new throw routes the turn into `chat.ts:5883-5906`, where `activeSessionOrch && allowMemoryPersistence && message.trim().length >= 8` writes the raw user turn as a `'user_stated'` frame (`frames.createPFrame`/`createIFrame`, 5900-5901) and emits `log.info('[chat] persisted raw user turn to memory despite generation failure')` at 5902. `grep 'despite generation failure'` over `packages/server/tests` → **zero hits**; the range is in neither the Map nor the Backlog. Pin the frame count on the workspace mind for a ≥8-character message, and pin the sub-8-character case separately. Also pin `chat.ts:5790-5811`: the turn's trace finalizes with `outcome: 'abandoned'` and `correctionFeedback: retainedTurnText(errMsg).slice(0, 500)` carrying the new message.

*Pin 3 from the scout (corrupt `config.json`) — **dropped.*** Two refuters: it mutates shared fixture state while the server is live (EBUSY/ordering flake on Windows) and pins a `WaggleConfig` failure mode through the chat route, which is the accidental coupling the phase is removing. G2 alone satisfies Rule 8 for the catch.

**P4-10b · T · TD-CHAT-30 direct pins** — new `describe('poisoned payload')` in `chat-governance.test.ts` (where the `@waggle/core` mock already lives): first call → `undefined`, fetch once, `// QUIRK`; second call within the TTL → `rejects.toThrow(TypeError)`, fetch still once; TTL-expired refetch failure → throws from line 59. Unique `workspaceId` per test.

**P4-18 · B · the fix.** Replaces **`chat.ts:4046-4060`** (4046 comment, 4047 `let`, 4048 `if`, 4049 `try`, 4050-4054 call, 4055 `throwIfTurnAborted()`, 4056 `} catch {`, 4057 `throwIfTurnAborted()`, 4058 comment, 4059 `}`, 4060 `}`). The unpinned catch alone is 4056-4059.

```ts
// Governance policies for team workspaces — direct call (no HTTP loopback)
let governancePolicies: { blockedTools?: string[]; allowedSources?: string[] } | undefined;
if (wsConfig?.teamId && effectiveWorkspace) {
  try {
    governancePolicies = await getGovernancePermissions(
      server.localConfig.dataDir, effectiveWorkspace, wsConfig.teamRole,
    );
    throwIfTurnAborted();
  } catch (err) {
    throwIfTurnAborted();
    // A governance lookup that throws is a programming or config fault,
    // not "no policy": refuse the turn rather than run it ungoverned.
    log.warn('[chat] governance policy lookup failed; failing the turn closed', {
      workspaceId: effectiveWorkspace, sessionId,
      error: err instanceof Error ? err.message : String(err),
    });
    throw new Error('Team governance policies could not be verified for this workspace. Try again or contact your team admin.');
  }
}
```

Shape notes: `throwIfTurnAborted()` stays first so a cancel during the fetch keeps abort precedence (5812-5814 handles `turnSignal.aborted` with no assistant error turn). `log.warn(message, { workspaceId, sessionId, error })` matches the ratified reference at **5726-5730** (5725 is `if (responseCommitted) {`; Convention 138 cites it as 5703-5707 at `b248ce38`). The message deliberately matches none of the classifier substrings at 5824-5849, so it reaches the verbatim branch at **5843** and `sendEvent('error', …)` at **5852**, with `GENERATION_FAILED_PREFIX` persistence at **5858-5869**. `responseCommitted` is set only at 5659, far after 4048, so the pre-commit branch is taken.

**After-fix observables (the complete set — the scout listed only the first two):** the runner is not invoked; exactly one SSE `error` with that message and **no** `done`; status 200; a `log.warn` with the original `TypeError` text; the session history gains `${GENERATION_FAILED_PREFIX}<message>`; **the trace finalizes `outcome: 'abandoned'` with the message in `correctionFeedback` (5790-5811)**; and **for a ≥8-character message the raw user turn is written to the mind with a second log line at 5902 (5883-5906)**. Pin G1 unchanged; G2's turn-2 assertions are edited deliberately in this same commit.

**Scope honesty — required in the commit body and the ledger.** This closes only the **thrown** half. `chat-governance.ts:34-36` (no team server) and `:57-61` (network failure / non-2xx / timeout) still return `undefined` = allow-all, and that is **pinned** at `chat-governance.test.ts:255-281` and `:315-349`. The post-fix contract sentence is therefore: *"a team turn refuses to run when the governance lookup throws, and still runs ungoverned when it fails softly (TD-CHAT-23, open)"* — **not** "a team turn runs governed or not at all".

**Complexity label: NEUTRAL on overall complexity; LOWERING on security unknown-unknowns.** Not "lowering" — the commit adds ~8 lines of error plumbing to a 6024-line handler, adds a third home for the fail-closed-`'critical'`/refuse decision, and adds one more entry to the TD-CHAT-15 verbatim-passthrough list, while leaving the actual shallow interface (`undefined` overloaded across five facts) untouched. **Record explicitly that this is an interim tactical step** whose end state is the discriminated return inside the 77-line module (**F3**) — that version deletes the caller-side try/catch instead of growing it.

Also in scope: update the helper's own doc comment at `chat-governance.ts:20-26` ("Returns the role-specific policy if found, or undefined if governance is unavailable"), which after this fix misdescribes both the unconfigured case and the throw path. Comment rides the behavior commit.

### 4.2 Trust half (`chat.ts:3663-3673`) — blocked

**Prerequisites, in order:** (1) re-verify §4.0; (2) **F2**; (3) the P4-06 approval-hook pins, which must include the `install_capability` happy path through 3635-3657 — otherwise the later removal of the 3697-3702 fallbacks silently changes the install branch's `summary`/`riskLevel`/`approvalClass` source; (4) the corrected Backlog mapping (**+142**: `3499-3532` = HEAD **3641-3674**, `3591-3619` = **3733-3761**, `3432-3458` = **3574-3600**, `3469-3484` = **3611-3626**).

**Coverage correction that must reach TESTING.md:** "no pinned behavior executes `chat.ts:3524-3762`" is **false** for 3524-3568 — `sse-resilience.test.ts` drives `edit_file` (ALWAYS_CONFIRM) at `autonomy: {level:'yolo'}` and asserts the write landed, which can only happen through the auto-pass return at 3564. The true unexecuted boundary is **3602-3762**.

**Fix shape (when unblocked):** a `catch (err)` binding `err`, a `log.warn` in the ratified object form with `workspaceId: executionScopeId`, and a conservative `trustMeta` of `{ riskLevel: 'critical', approvalClass: 'critical', assessmentMode: 'heuristic', description }`. The `description` must **not** call `describeToolUse` — that is the plausible thrower and 3697 would re-invoke it unguarded. **Do not invent a new phrase**: either reuse `describeToolUse`'s own default vocabulary (`Using ${name}…`, `chat-helpers.ts:1552`) or, better, add `describeToolUseSafe(name, input)` beside it and use it at 3671, **3697** and **4649** — one total helper that also closes TD-CHAT-36 and TD-CHAT-37 without a second inline string.

**Do not fold in:** the 3697-3702 dead-fallback removal (its only evidence would be a seam-only pin; 3701 is the sole consumer of `grantRiskLevel` outside 3612/3617) or the first-catch log at 3656 (P4-20).

**Label: RAISING as scoped** (+~18 lines, a duplicated `trustMeta` literal, a third home for the `'critical'` default alongside `approval-grants.ts:44-46` and `chat.ts:3698-3701`); lowering only if the 3634/3697-3702 collapse to a non-optional typed `trustMeta` lands in the same arc. **Name and reject the two cheaper alternatives in the proposal text**: (a) delete the try/catch entirely — already fail-closed via `hooks.ts:91-93` + `tool-executor.ts:184-190`, rejected on observability grounds only (silent `[BLOCKED]` vs a visible critical card + a log); (b) the deep version, a total `resolveApprovalTrustMeta(toolName, input, trustedRiskLevel): ApprovalTrustMeta` replacing 3628-3702 with one call, which makes the collapse free.

### 4.3 Collateral to pin + ledger (never fix in these commits)

TD-CHAT-30 (poisoned cache), TD-CHAT-36 (unguarded model-arg coercion — full HEAD site list **3587, 3654, 3671, 3697, 4649** plus `approval-grants.ts:80/85` and `confirmation.ts:297/309`; 3587 is the site both scouts omitted), TD-CHAT-37 (`hooks.ts:88-94` swallow → orphan card), TD-CHAT-38 (first-catch missing log), TD-CHAT-39 (`chat.ts:3733` `log.warn` string form inside the newly pinned region). Also record that `canAlwaysAllow(undefined) === true` is **pinned client-side** (`apps/web/src/test/p7-a5-approval-card-risk.test.tsx:35`) and will not change — the server contract is "`approvalClass` is always present" — and that a `'critical'`-labelled held row stays executable on explicit approve (`held-action-executor.ts:181` re-validates on args, not the stored class), so the label gates the UI and grant persistence only.

---

## 5. Draft docs rows

### 5.0 ID allocation (superseded — see the header note; five scouts each claimed `TD-CHAT-30`)

Highest existing at HEAD: **TD-CHAT-29**, **TD-TEST-7**, **TD-REL-5**, **TD-ENV-1**.

| New id | Subject | Proposed by |
|---|---|---|
| TD-CHAT-30 | `chat-governance.ts` poisoned-policy cache | gov-pin |
| TD-CHAT-31 | dangling `chat.turn.start` (4 exits) | lens-workspace-resolution |
| TD-CHAT-32 | `usesNamedWorkspace` cross-wiring | lens-workspace-resolution |
| TD-CHAT-33 | dead write `workspacePath = resolved` (2782) | lens-workspace-resolution |
| TD-CHAT-34 | personal-scope artifacts never indexed | lens-cross-module |
| TD-CHAT-35 | absent-workspace command context by route + mind mix | lens-cross-module |
| TD-CHAT-36 | unguarded model-arg coercion | trust-pin |
| TD-CHAT-37 | `HookRegistry.fire` swallow → orphan approval card | trust-pin |
| TD-CHAT-38 | install_capability first-catch missing log | trust-pin |
| TD-CHAT-39 | `chat.ts:3733` `log.warn` string form | trust-pin refuter |
| TD-CHAT-40 | dead guard `gop_id: sessionId ?? 'unknown'` (4810) | lens-turnscope-seam |
| TD-CHAT-41 | stale comment at 3796 asserting route-test unreachability | lens-turnscope-seam refuter |
| TD-CHAT-42 | canned-reply `raw.end()` missing the destroyed/ended guard | lens-validator refuter |
| TD-TEST-8 | pin-domain over-approximation in the command-context pins | lens-validator |

**Not allocated:** the `WAGGLE_MAX_MESSAGE_LENGTH` per-call read (fold into TD-CHAT-5 as an amendment — same line, same single remedy, and Convention 145 already mandates it); `CHAT_TURN_IN_PROGRESS` vs `SESSION_TURN_IN_PROGRESS` (folded into TD-CHAT-36's neighbourhood as a **scope table with a do-not-unify guard**, not a synonym row).

### 5.1 Smell Inventory rows (`| Smell | Location | Refactoring | Status |`)

```
| Information Leakage — governance "no policy = allow all" is represented three ways across three packages (`undefined` ×5 causes, `{blockedTools: []}`, `Set(0)`) | `chat-governance.ts:15-18, 31, 34-36, 57-61, 69-76`; `routes/chat.ts:4047, 4065, 4212, 4592`; `agent-loop.ts:127-131, 1782`; `tool-executor.ts:59, 129`; `subagent-orchestrator.ts:455`; `subagent-tools.ts:432`; `team-capability-governance.ts:107, 113` | Make the return total (`readonly blockedTools: readonly string[]`), normalise once at agent-loop config intake; drop `allowedSources` (declared at 17, never populated) | ticketed TD-CHAT-23 / P4-X6 — Phase 5/6, blocked on route-level pins of the `undefined` and `[]` paths |
| Temporal Decomposition — the chat-history recovery gate is re-implemented per Fastify plugin; `chat.ts:1860-1870` is byte-equivalent to `agent.ts:20-29` and the `=== 'default'` precondition is spelled three times | `chat-persistence.ts:126-132, 308-314, 393-437`; `routes/chat.ts:195, 231-243, 1860-1873, 2175-2177, 5976-5983`; `routes/agent.ts:20-29, 110-117`; `routes/backup.ts:497-503` | Owner exports `createChatHistoryLayoutGate(dataDir) → { statusFor(historyWorkspaceId) }` with an eager `reset()`; `backup.ts` keeps its per-request probe (a cached gate there is fail-open) | ticketed — Phase 6, blocked on Safety Net Map rows for `agent.ts` / `backup.ts` / `chat-persistence.ts` |
| Stringly Contract — the command-context result vocabulary is 3 producers × 13 literal sites × 6 distinct strings against 1 consumer × 6 equality checks × 3 strings; three sentinels are invisible to every handler by construction | `routes/chat.ts:370, 373, 377, 381, 383, 385, 393`; `routes/commands.ts:67, 71, 83`; `routes/command.ts:259, 263, 274`; `agent/src/commands/workflow-commands.ts:27, 35, 54, 62, 203, 218` | Step 1: one `COMMAND_CONTEXT_SENTINEL` `as const` in `command-registry.ts` **and re-exported from `packages/agent/src/index.ts:388`**, all three producers + the consumer in one commit. Step 2: discriminated `CommandSource` (`ok`/`empty`/`denied`/`unavailable`) — supersedes TD-CHAT-1, Phase 5 | ticketed TD-CHAT-28 — blocked on sibling-route pins + Map rows |
| Primitive Obsession — `executionScopeId` is one string carrying four roles (audit/signal stream key, spend-budget key, MCP tool-pool key, filesystem path segment) across ~20 call sites | `routes/chat.ts:62, 227, 5776`; audit 3472/3556/3687/3735/3756/3760/4658/4728/5379; signals 3318/4653/4888/5665; budget 4188/4579; MCP 3870; collaboration 4197; filesystem 4757/4762/4767 | Do **not** split into two names (the split already exists at 224 vs 227 and enforces nothing). Fold into `ChatTurnScope`; fix the filesystem role as TD-CHAT-34 | ticketed TD-CHAT-3 |
| Duplicate Code — the ~25-line per-request orchestrator-lease block is verbatim in both palette routes | `routes/commands.ts:33-57` ≡ `routes/command.ts:229-251` | Extract after both routes are pinned and on the Safety Net Map | ticketed TD-CHAT-35 — Phase 6 |
| Inconsistent Termination — 2 of 8 `raw.end()` sites omit the `if (!raw.destroyed && !raw.writableEnded)` guard used everywhere else | `routes/chat.ts:3289, 3294` vs `2926, 3121, 3136, 5731, 5820, 5951` | Unify the guard when the region is next touched; deliberate per-site `raw.end()` placement from `9bb544a7` is unrelated and stays | ticketed TD-CHAT-42 |
```

**Amend in place:** row 74 (`TurnScope` → `ChatTurnScope`; keep "~20 call sites" — its scope is the three-value subset, so do not overwrite it with the 167 census, which belongs on row 86); row 86 (`TurnScope` → `ChatTurnScope`; anchors `2619-2627, 2870-2878, 2938-2940` at HEAD alongside the `b248ce38` numbers; census; note that the inline is **subsumed by** the method object, not a second named transformation); row 87 (`2879 (+36 reads)` → `2931 (+35 handler reads: 27 `!hasCustomRunner` gates, 8 positive; comment 3796; `661-667` is the helper, outside the handler)`); row 75 (7-field → 6-field signature after P4-11, 5 after P4-12); row 71 (`~2568–2573 at Phase 1 numbering` → `2760-2765` at HEAD).

### 5.2 New Debt Ledger rows (`| Item | Location | Type | Risk | Effort | Priority | Status |`)

```
| TD-CHAT-30 A policies array containing a `null` element is cached before it is validated (`policyCache.set` at 55 precedes `extractRolePolicy` at 56), so the first call swallows the TypeError into `undefined` (allow-all) and every call within the 5-minute TTL throws `TypeError: Cannot read properties of null (reading 'role')` from line 42, **outside** the try; after TTL expiry a failed refetch throws again from line 59. Design defect: `extractRolePolicy` is called on unvalidated wire data from three sites (42, 56, 59), two outside the try — validation belongs once, at the parse point (53) | `routes/chat-governance.ts:39-42, 45-61 (53, 55, 56, 59), 69-76` | security fail-open (latent) + cache poisoning | high — `chat.ts:4056` swallows it today into an ungoverned turn; after the P4-18 fix every turn on that workspace fails for 5 minutes | S | P1 | open; pins in `chat-governance.test.ts` (P4-10b); fix (validate before caching) in its own behavior commit with a deliberate re-pin |
| TD-CHAT-31 `chat.turn.start` (2645-2650) is emitted between the Target and Paths gates, so the four post-log pre-hijack exits mint a turnId with no downstream stage event while Target-class rejections mint none. There is no `chat.turn.end` stage anywhere (terminal stage is `agent-loop.exit`), so the defect is placement, not a missing counterpart; the event's `workspace:` field names a workspace the turn never executed in | `routes/chat.ts:2644-2650` vs exits `2662, 2742, 2761, 2777` (hijack at 2787); `agent/src/turn-context.ts:45-63` | quirk (suspected) — audit traceability | low | S | P3 | open; pinnable today via `startTurnCapture` (P4-09) — no `packages/server` test imports it; any fix is a behavior commit covering all four exits and reverses the H-AUDIT-1 "turn entry" decision (founder F4) |
| TD-CHAT-32 `usesNamedWorkspace` (222) describes the **history/authorized** workspace but selects the **body** workspace's root (293-295); for body `'default'` + no managed default + active workspace A, the legacy branch (296-298) is skipped, so an out-of-dataDir `explicitWorkspacePath` does not reach the 400 `PATH_TRAVERSAL` it would get with no active workspace. No escape (302-340 overrides execution to A's root) — a status-code dependence on session state | `routes/chat.ts:222, 293-295, 296-298, 301, 302-340, 2770-2783` | quirk (suspected) | low | S | P3 | open; both legs unpinned — `PATH_TRAVERSAL` has zero test hits repo-wide |
| TD-CHAT-33 `workspacePath = resolved` is a dead write: every downstream consumer reads `executionWorkspacePath`, which was captured pre-`path.resolve` at 301; the token has no reader after 2782 (3764 is a comment). The traversal check at 2770-2781 still guards the same string, so no security hole | `routes/chat.ts:2782` (readers 2963, 3010, 3021, 4166-4169, 4393) | dead write | low | XS | P3 | open; covers only the assignment — the traversal check stays |
| TD-CHAT-34 A no-active-workspace turn passes the `personal::default` sentinel into `artifactsFilePath`, which joins it into `dataDir/workspaces/<id>/artifacts.json`. On Windows `mkdirSync` throws ENOENT (`:` illegal in a path segment) and `chat.ts:4769-4771` swallows it, so generated docx/pdf/xlsx/pptx are never **indexed** as Library artifacts (the files themselves are still written); on POSIX the index is written but unreadable — `workspaceIds()` enumerates only `workspaceManager.list()` ids, and an explicit `?workspaceId=personal::default` is a 400 via `assertSafeSegment`. Reachable by default: `index.ts:1399` initialises `activeWorkspaceId = null` and `security-middleware.ts:605-614` resolves an unnamed `/api/chat` turn to it | `routes/chat.ts:621-641, 4737-4771`; `routes/artifact-index.ts:26-28, 44-52`; `routes/artifacts.ts:81, 85, 106-112`; `local/validate.ts:5-7` | bug (confirmed by probe) | medium — Library completeness on the Windows Solo target | S | P2 | open; pin via the `agentRunner` `onToolResult` seam (no `hasCustomRunner` gate between 4666 and 4771), Windows/POSIX split; deep fix = `artifact-index.ts` asserts its own path constraint; personal-key decision is TD-CHAT-26 (founder F7) |
| TD-CHAT-35 Three routes answer "what workspace does a slash command see when none is named" three ways: chat resolves through the middleware (explicit id → `agentState.activeWorkspaceId` → null) and labels `Personal` with a no-state sentinel; `commands.ts:27` and `command.ts:223` fall back to the literal `'default'` and call `buildWorkspaceNowBlock` for it. Sharper defect: with no workspaceId the guard at `commands.ts:39` / `command.ts:229` is skipped, so `commandOrch` stays the shared **personal** orchestrator while `getWorkspaceState` reads the managed **default** workspace's mind — one CommandContext straddling two minds | `routes/chat.ts:62-67, 367, 385, 393`; `routes/commands.ts:27, 31, 39, 75-85`; `routes/command.ts:223, 227, 229, 266-276`; entry `apps/web/.../CommandCenter.tsx:404, 459` → `adapter.ts:3331` | duplicated knowledge + cross-mind mix | medium — cross-mind mixing is a standing project invariant | S | P2 | open; the **label** divergence is unconditional, the **state** divergence is conditional on a registered `default` workspace; both sibling routes are off the Safety Net Map; `assertSafeSegment` accepts `'Personal'`, so a naive single producer would silently resolve `wsManager.get('Personal')` |
| TD-CHAT-36 Model-supplied tool args are coerced by template literal / `String()` at five chat.ts sites and two helper modules with no guard; a JSON-producible `{"toString":0}` makes ToString throw TypeError. Related: `CHAT_TURN_IN_PROGRESS` (`backup.ts:494`, dataDir-wide via `isChatHistoryRestoreBusy`) and `SESSION_TURN_IN_PROGRESS` (`chat.ts:2924, 5995`, `sessions.ts:306`, per-session) are **different predicates at different scopes** — both are asserted wire contracts and must not be unified | `routes/chat.ts:3587, 3654, 3671, 3697, 4649`; `agent/src/chat-helpers.ts:1465-1564`; `routes/approval-grants.ts:80, 85`; `agent/src/confirmation.ts:297, 309` | latent crash / unguarded coercion | medium | S | P2 | open; remedy = one total `describeToolUseSafe(name, input)` used at 3671, 3697 and 4649; reachability of the child-run path is the open question in TD-CHAT-23 (founder F2) |
| TD-CHAT-37 `HookRegistry.fire` swallows any hook exception with no log (the comment says "log but continue"; nothing logs). Combined with the unguarded `describeToolUse` at `chat.ts:3697` — after `approval_required` was emitted at 3680 and `approval_requested` audited at 3685-3691 — a throw leaves an orphan approval card (`POST /api/approval/:id` 404s, `pendingApprovals` never set) while tool-executor step 4b blocks the tool with no `approval_denied` audit | `agent/src/hooks.ts:88-94`; `routes/chat.ts:3680-3697`; `agent/src/tool-executor.ts:178-204` | silent swallow / orphan state | medium | S | P2 | open; severity depends on the TD-CHAT-23 reachability verification |
| TD-CHAT-38 The install_capability trust-assessment catch falls through to the heuristic block (so it is **not** fail-open) but emits no log, against the ratified swallow convention; reachable in production via `path.basename` `ERR_INVALID_ARG_TYPE` on a non-string `name` and via fs EISDIR/EACCES | `routes/chat.ts:3641-3657` (catch at 3656) | missing convention log | low | XS | P3 | open; its own commit and its own pin, never mixed with the TD-CHAT-23 fail-closed commit |
| TD-CHAT-39 `log.warn` uses the string form rather than the ratified `log.warn(message, { workspaceId, sessionId, error })` object form, inside a region the TD-CHAT-23 pins newly cover | `routes/chat.ts:3733` | convention deviation | low | XS | P3 | open; record, do not fix in the pin commit |
| TD-CHAT-40 `gop_id: sessionId ?? 'unknown'` is unreachable — `sessionId` (2938 ← 2870) is typed `string` via `ChatHistoryTarget.workspaceId` and is never nullish | `routes/chat.ts:4810` | dead defensive guard | low | XS | P3 | open; inside the unpinned TeamSync region — do not delete before the P4-08 pins; mark `// QUIRK` when the region is touched |
| TD-CHAT-41 An in-code comment asserts the persona-tool-filter block is "`!hasCustomRunner`-gated and therefore unreachable from route tests"; `sse-resilience.test.ts` and `persona-acceptance-prompt-budget.test.ts` post to `/api/chat` with no `agentRunner`, so the block **is** reachable from route tests | `routes/chat.ts:3796` | stale comment (confirmed) | low | XS | P3 | open; correct with the next pinned change nearest the site (TD-CHAT-19 vocabulary) |
| TD-CHAT-42 Two of eight `raw.end()` sites omit the `if (!raw.destroyed && !raw.writableEnded)` guard used at the other six; reachable if the socket is destroyed between the 3236 abort check and the call. The terminal-turn protocol for canned replies is one decision spread over 3288-3290, 3293-3295, 3308 and the outer `finally` at 5907/5951 | `routes/chat.ts:3289, 3294` vs `2926, 3121, 3136, 5731, 5820, 5951` | inconsistent guard | low | S | P3 | open; the per-site `raw.end()` placement preserved in `9bb544a7` is a different, deliberate decision and stays |
| TD-TEST-8 The command-context pin fixture constructs states the route cannot produce — `executionWorkspaceId ≠ effectiveWorkspace` (test 89 vs 163-209, provably one value at 224-226 → 2624 → 2872 → 2939) and `persistedMemoryReadAllowed` inconsistent with `turnMutationPolicy` (test 182 vs the derivation at 80). The pins remain valid observations of the helper but over-approximate its production domain; after the collapse the fixture can no longer express the divergence, retiring an implicit pin by design | `tests/local/chat-command-context-characterization.test.ts:77-82, 89, 163, 176, 182, 191, 197, 209` | test-domain over-approximation | low | XS | P3 | closes with P4-11 / P4-12; record the retirement rather than dropping it silently |
```

### 5.3 Amendments to existing rows

- **TD-CHAT-5** — location `routes/chat.ts:113` → **`:122`**. Append: *"the same line is a hidden configuration input inside an otherwise pure validator; the guard fix hoists the parsed limit to a module-scope constant per Convention 145, or records why a per-call read is kept (note: no test stubs this env var today). Read timing is the inverse of TD-CHAT-11's module-load capture — name the tension before choosing."* Do **not** open a separate row.
- **TD-CHAT-9** — location → **`chat.ts:311-319, 2760-2765; security-middleware.ts:895-903` at `ea82185d`** (the current `2429-2443, 2568-2573` are journey-start numbers; `b248ce38:2429-2443` is `buildTurnContextSuffix`, unrelated). Reclassify **per site**: 311-319 is a defense-in-depth duplicate, unreachable behind the unconditional `fp`-wrapped preHandler except across a within-request `workspace.json` role change (`WorkspaceManager.get` re-reads and re-parses per call); 2760-2765 is unreachable **conditional on the composition invariant** that `securityMiddleware` (`index.ts:2886`) precedes `chatRoutes` (`index.ts:2965`) — the only registration. Record the wire-text difference (`'…cannot modify…'` vs `'…cannot send messages…'`, same code) so a Phase 6 single-gate decision is understood as a body change for the TOCTOU case.
- **TD-CHAT-10** — anchors → `runAgentAttempt` **4946**, `runModelFallbackChain` **5112**, `runPrimaryWithSafeInterruptedRetry` **5170**, credential rotation **5258**.
- **TD-CHAT-16** — see §5.1 row 87; add the migration inventory (119 `agentRunner =` assignment **lines**: chat-api 82, smart-router-chat 18, local-mode 8, team-integration 4, chat-route-characterization 3, workspace-api 2, backup-restore 2) and correct the blocker: those assignments **assert on the injected object**, which is why they must be rewritten against the `runAgentLoop` mock's `config`; the gates themselves already execute under that mock.
- **TD-CHAT-18** — precondition widens from "after the 403 helper site is pinned" to **"after all five unpinned code-emission sites are pinned: 311-319, 2606 (`SESSION_ID_CONFLICT`, a named row-54 Gap, zero hits), 2744 (`INJECTION_DETECTED` — the only test hit is a stubbed fetch body), 2763, 2779 (`PATH_TRAVERSAL`, zero hits)"**. Note that `CHAT_HISTORY_RECOVERY_CODE` is **forwarded off the returned status object** at 238/5981/`agent.ts:115`/`backup.ts:501`, not imported anywhere — the TD-CHAT-18 commit introduces the first such import.
- **TD-CHAT-20** — anchors → **3288/3293/3308** (delays) and **374** (`slice(0, 5)`, now inside `buildChatCommandContext`, not 3212). Add: *Convention 145 exempts `.slice(0, n)` as a stdlib idiom, so the recall-cap half is arguably already declined — founder ruling F8 before the constants commit.* Also: the three delay sites are **fragment-pinned** (the code paths execute), but **no test asserts 10 vs 15 ms**.
- **TD-CHAT-23** — locations → `routes/chat.ts:3659-3670` region and **`4046-4060`** at HEAD (b248ce38 `3642-3650`, `4026-4033`); split status into the two halves; state that the chat.ts governance fix closes only the **thrown** half and that the helper's own network/non-2xx → `undefined` allow-all remains pinned at `chat-governance.test.ts:255-281` and `:315-349`.
- **TD-CHAT-26** — add: *"the helper doc at HEAD `256-262` states the `never the user's home directory` guarantee that the callers break at 3010 and 3021; the doc is therefore an artifact of this row and may not be edited before the decision."*
- **TD-CHAT-28** — locator `3208-3249` → **`350-402` + `3271-3274`**; counts → **3 producers × 13 literal sites × 6 strings** vs **1 consumer × 6 comparison sites × 3 strings**; exclude `chat.ts:3272` from the vocabulary (it **replaces** `commandRegistry.execute(...)` in a ternary, so no handler can observe it); record that step 1 also requires the re-export at `packages/agent/src/index.ts:388` and converts `commands.ts:13` from a type-only to a value import; record the **five** contracts that share wording — command-context (period), `search_memory` tool `tools.ts:282` + `result-formatter.ts:64` (period, same package as the const's proposed home), cross-workspace `tools.ts:327` (period, superstring — a substring sweep hits it), `auto_recall` SSE `chat.ts:3399` (**no period**, pinned `durable-memory-restart.test.ts:223`), and the `/memory` heading. Correct the false pin: `apps/web/.../chat-work-canvas.test.tsx:147` is an `it.each` **input row** and `BlockRenderer.tsx:292-293` matches a case-insensitive regex, so it is period-agnostic and pins nothing. Reconcile with Convention 140 (**F10**).
- **TESTING.md Seam caveat** — `runAgentAttempt 4806` / `runModelFallbackChain 4967` are base-`44baa77d` coverage starts; the declarations are 4804/4970 at base and **4946/5112 at HEAD**. State which base each pair uses.

---

## 6. Founder ratification list (Rule 5)

| # | Decision | Agent recommendation |
|---|---|---|
| **F1** | Convention 138 says a security-boundary swallow "fails closed **instead of** logging". P4-18 does both. | **Amend the bullet**: fail-closed is the boundary rule; a `log.warn` carrying the original cause is required, not optional — the turn now fails and the operator needs the `TypeError` text. Do not ship an unreconciled deviation. |
| **F2** | Is `chat.ts:3673` reachable in production via child sub-agent/workflow `bash` calls (§4.0)? If yes, P4-19 converts an always-denied critical shell call into an operator-approvable one. | **Re-verify the chain first** (one refuter proved it; two did not test it). If reachable: this is a security-posture change and the headline of the commit, not a footnote. Do not schedule P4-19 on either reading alone. |
| **F3** | Governance fix direction: caller-side fail-closed now (P4-18), or the discriminated return inside `chat-governance.ts` first? | **Caller-side first** (smaller blast radius, Rule-8 cheaper), **recorded as an interim tactical step** whose end state is the discriminated return — which deletes the caller's try/catch instead of growing it and closes TD-CHAT-23 in full. |
| **F4** | Turn-trace generation point: leave `chat.turn.start` at 2644-2650, move it below the resolvers, or move it to true entry (above both, honouring H-AUDIT-1)? | **Leave it for Phase 4**; pin it (P4-09) and ledger TD-CHAT-31. If reopened, prefer **true entry** — it makes the invariant uniform across all four exits and matches the ratified design doc; it changes the `workspace:` field, so it is a behavior commit with its own pin. |
| **F5** | Helper merge shape: full merge (P4-WS-5), admissibility-vs-roots cut (P4-WS-6), or keep both? | **Keep both for Phase 4.** Decide in Phase 6 after P4-17 and TD-CHAT-18. If merging, the honest cut is **admissibility vs roots**, not a single 11-field helper. |
| **F6** | TD-CHAT-9: delete the duplicate viewer sites, or keep as defense in depth? | **Keep.** Deletion is a behavior change for the TOCTOU case and, for 2760-2765, depends on a composition invariant rather than on the code. |
| **F7** | TD-CHAT-26: what is the personal-scope root/library key? Gates P4-WS-8b **and** the TD-CHAT-34 fix. | For TD-CHAT-34, **skip indexing when `executionWorkspaceId` is undefined** (lowers complexity — stops a non-workspace value entering a path-joining interface). "Index under a real personal key" **raises** it (a second id namespace every reader must learn). |
| **F8** | TD-CHAT-20: is the `/memory` recall cap in scope, given Convention 145 exempts `.slice(0, n)`? | **Out of scope — exempt.** Only the three delay literals (3288/3293/3308) remain actionable. Record the ruling so the two rows stop contradicting each other. |
| **F9** | P4-VAL-2: defer, or take the key-checked variant? | **Defer.** Ratify that the validator's input type keeps compile-checked wire-name mirroring (Convention 142). Revisit after TD-CHAT-18. |
| **F10** | TD-CHAT-28 step 1 vs Convention 140 ("human-readable `error` messages stay inline"). | **Ratify the exception**: these strings are compared by equality across a package boundary (`workflow-commands.ts` ×6), i.e. wire codes wearing prose, not display-only messages. Home is `@waggle/agent` (the interpreter), not `@waggle/shared`. |
| **F11** | P4-X5 wire-code map placement. | **Keep C3 as ratified** (map beside `ChatRequestRejection`, `chat.ts:69-72`). No leaf constants module. Codes owned elsewhere (`VIEWER_READ_ONLY` → `security-middleware.ts`; `SESSION_TURN_IN_PROGRESS` → the shared turn-state seam) stay with their owners. |
| **F12** | TD-CHAT-3 sequencing: Smell row 74 says "Introduce Parameter Object **once the method object exists**"; P4-A1 inverts it. | **Ratify the inversion** (the scope object shrinks the method object's future constructor surface from 8 ids to 1) **and amend row 74 in the same commit**, or keep the order and defer A1. Do not leave an unexplained inversion. |
| **F13** | `ChatTurnScope` snapshot locals vs Convention 142 (`const activeSessionId = scope.sessionId` is a destructure-rename in dot form). | **No re-snapshotting.** Bind once from `scope` at the existing 2938-2940 sites. Six new second names for the same values would make the in-handler chain three layers deep and raise, not lower, cognitive load. |
| **F14** | Rule 8 clarification: is a tsc-checked identity-alias inline exempt? | **Ratify as proposed (agent-proposed framing):** no. Line-based rule, no proof-of-equivalence carve-out. Strengthened by the finding that tsc is **not** a complete oracle for shorthand properties into untyped sinks. |
| **F15** | Assertion-surface constraint for the Wave 2 pin program. | **Ratify.** Pins assert HTTP/SSE/persisted/audit-store observables only; never handler locals, never `emitAuditEvent` call args by field name. Without it, ~30 new pins fossilise the leak TD-CHAT-3 exists to remove — exactly what TD-TEST-8 records already happened once. |
| **F16** | Does `backup.ts` adopt the chat-history gate (P4-X1)? | **No.** Its per-request probe is the freshness guarantee on the one route that mutates the dataDir under any cache. |

---

## 7. Must re-verify before writing (unverified in the panel)

No claim carried `verdict=INCOMPLETE`; every claim got all three lenses. These are unresolved **facts**, not unresolved panels.

1. **TD-CHAT-23 trust-half reachability** (F2) — one refuter's child-run chain vs two refuters accepting the scout's unreachability proof. Blocks P4-19 and the severity of TD-CHAT-36/37. Verify by reading `chat.ts:4210-4213`, `chat-collaboration.ts:718-745`, `subagent-tools.ts:429-439`, `confirmation.ts:203, 284, 296-297`, `system-tools.ts:596-600`.
2. **Fetch-spy harness size.** Three incompatible measurements of the same thing: "7 `globalThis.fetch =` stub bodies", "5 tests / 7 stubs", "8 `vi.spyOn` + 7 direct = ~15". TESTING.md:36 says 8. **Re-measure and state the counting method** before amending the Seam caveat.
3. **Alias census totals.** "12 names / ~157 reads" (scout) vs "13 names / ~167 reads" (refuter); "113 alias lines" agreed. Re-derive with the recorded command before it enters row 86.
4. **Backlog→HEAD mappings for 2880, 3591, 4650, 5266.** The +142 constant applied to the Backlog's printed numbers gives 3072/3733/4792/5408; the first scout's approximate +140 gave 3071/3732/4790/5396. Both are internally consistent once you account for the printed numbers being coverage starts 0-12 lines before the declaration — **re-anchor by symbol** (P4-01) rather than arbitrating the arithmetic.
5. **`smart-router-chat.test.ts:117-211` and `:113`** — if these genuinely pin managed-workspace artifact indexing and `spendWorkspaceId`, TD-CHAT-34's gap list shrinks and both belong in Map row 58. Check before writing P4-02 and TD-CHAT-34.
6. **A combined-invalidity probe for P4-12's replacement pin** — does any single directive yield both `denyMemoryRead` and `denyConversationHistory`? If not, P4-12 becomes no-change and the two-encodings smell is ledgered instead.