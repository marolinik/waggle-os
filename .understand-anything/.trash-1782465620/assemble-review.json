{
  "fixedSectionOk": true,
  "nodesRecovered": 4,
  "edgesRestored": 7,
  "crossBatchEdgesAdded": 0,
  "typesRemapped": 0,
  "complexityRemapped": 0,
  "notes": [
    "Fixed section is sane. The 312 corrections were dominated by tested_by direction normalization (257 flipped test->prod into prod->test; 55 dropped as orphan or same-domain test<->test / prod<->prod pairs) plus a tested-by linker pass (1 produced, 498 production nodes tagged 'tested'). No ID-correction skew and no complexity skew to flag; node-type and complexity distributions look healthy.",
    "Recovered 4 nodes the file-analyzer never emitted: 3 functions (escapeXml in packages/server/src/local/routes/harvest.ts; InlineApprovalCard in apps/web/src/components/os/warm/InlineApprovalCard.tsx; EvidenceChip in apps/web/src/components/ui/evidence-chip.tsx) and 1 test file (packages/server/tests/workspace-templates.test.ts). All verified to exist on disk; the 3 functions are real exported symbols (line ranges resolved from source) and the referencing edges are valid.",
    "Restored 7 edges anchored on the recovered (and one pre-existing) nodes: escapeXml 'calls' (1); InlineApprovalCard 'contains'+'exports' (2); EvidenceChip 'contains' (1); workspace-templates.test.ts 'imports' x2 (2); onboarding-profile.test.ts -> onboarding-profile.ts 'imports' (1). The onboarding-profile 'tested_by' edge (restored as prod->test) already survived the merge, so it was a no-op, not double-counted.",
    "DATA-QUALITY FIX: the pre-existing node file:apps/web/src/lib/onboarding-profile.test.ts had a wrong filePath field (it pointed at the production file apps/web/src/lib/onboarding-profile.ts, not the test file). Corrected the filePath to match its id. This filePath/id mismatch is likely why the merge classified the test<->prod pair as prod<->prod and dropped its tested_by edge.",
    "Cross-batch import gap check is CLEAN: of 3088 importMap pairs, 3085 have both file: nodes present and ALL 3085 already had their imports edge in the graph. The only 3 pairs missing an edge were due to a missing SOURCE node (workspace-templates.test.ts x2, onboarding-profile.test.ts x1) -- all resolved by node recovery above. Zero speculative cross-batch edges added.",
    "WARNING (left dropped, genuinely stale): pipeline:.github/workflows/deploy-www.yml -> file:apps/www/src/index.tsx ('deploys'). apps/www is a Next.js app (app/ directory); apps/www/src/index.tsx does not exist on disk and is not in the importMap. Correctly dropped.",
    "WARNING (left dropped, genuinely stale): document:docs/handoffs/2026-05-27-web-guidelines-review.md -> file:apps/web/src/components/os/apps/MemoryApp.tsx ('documents'). MemoryApp.tsx does not exist; the component was renamed/refactored to MemoryCenterApp.tsx. Correctly dropped.",
    "Root cause of all recoverable drops: the file-analyzer occasionally omits function-level nodes (and, rarely, a test file node) while still emitting edges that reference them. Rate is very low (4 recovered nodes / 4063 ~= 0.1%); no systemic corruption.",
    "Final integrity: graph now has 4067 nodes / 6521 edges; 0 dangling edges, 0 duplicate node ids, 0 duplicate edges."
  ],
  "warningsRemainingDroppedEdges": [
    "pipeline:.github/workflows/deploy-www.yml -> file:apps/www/src/index.tsx (deploys): target file does not exist on disk (apps/www is a Next.js app/ app, no src/index.tsx).",
    "document:docs/handoffs/2026-05-27-web-guidelines-review.md -> file:apps/web/src/components/os/apps/MemoryApp.tsx (documents): target renamed to MemoryCenterApp.tsx; stale reference."
  ],
  "dataQualityFixes": [
    "Corrected wrong filePath on node file:apps/web/src/lib/onboarding-profile.test.ts (was 'apps/web/src/lib/onboarding-profile.ts')."
  ],
  "integrity": {
    "finalNodes": 4067,
    "finalEdges": 6521,
    "danglingEdges": 0,
    "duplicateNodeIds": 0,
    "duplicateEdges": 0,
    "filePathFixed": 1
  }
}